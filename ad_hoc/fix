unique(e$component); unique(e$pop)

segs <- c("res", "office", "apt")

acc <- e[track %in% segs & component == "total" & pop == "seed_observed",
         .(track, horizon, anchored, n,
           WAPE     = round(100 * WAPE, 1),
           bias_pct = round(100 * bias_pct, 1))]
setorder(acc, track, horizon, -anchored)
acc

g <- read_bt("growth_by_horizon.csv")
names(g)
