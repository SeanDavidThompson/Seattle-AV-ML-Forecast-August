library(data.table)

read_bt <- function(f) {
  x <- readLines(file.path("data/outputs/backtest", f))
  fread(text = x[!startsWith(x, "#")])
}

e <- read_bt("errors_by_horizon.csv")
names(e); unique(e$track)
