gg <- g[track %in% segs & pop == "seed_observed",
        .(track, horizon, anchored,
          g_pred     = round(mean_g_pred, 2),
          g_act      = round(mean_g_act, 2),
          err_pp     = round(mean_err_pp, 2),
          cum_err_pp = round(mean_cum_err_pp, 2))]
setorder(gg, track, horizon, -anchored)
gg
