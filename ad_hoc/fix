file.remove(here::here("data", "cache", "area_report_actuals_2026.rds"))

MAIN_ML_DEFINE_ONLY <- TRUE
source(here::here("scripts", "ml", "main_ml.R"))

run_main_ml(
  replicate          = FALSE,
  panel_replicate    = FALSE,
  model_replicate    = FALSE,   # models from this run are saved
  retrofit_replicate = FALSE,   # retrofits are saved too
  extend_replicate   = TRUE,    # redo Step 5b; it crashed partway
  forecast_only      = FALSE,
  scenario           = "baseline",
  seed               = 456,
  use_area_actuals   = TRUE,
  area_reports_year  = 2026,
  train_through_year = NULL,
  cache_dir  = here::here("data", "cache"),
  model_dir  = here::here("data", "model"),
  output_dir = here::here("data", "outputs")
)
